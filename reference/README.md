# Referência

- **`images/`** — astrofotografias solares (H-alpha e outras) e o render
  de comparação, documentadas em `images/README.md`. Usadas como
  referência visual nas fases e rodadas de julgamento.

O app vivo está em `src/` (Vite). O monólito histórico (`sol3d.html` /
`sol-3d.html`) saiu da árvore; o histórico permanece no git.
